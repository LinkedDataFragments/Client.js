PREFIX dbpedia: <http://dbpedia.org/resource/>
PREFIX dbpedia-owl: <http://dbpedia.org/ontology/>

SELECT DISTINCT ?birthPlace WHERE {
  ?player dbpedia-owl:team dbpedia:Real_Madrid.
  ?player dbpedia-owl:birthPlace ?birthPlace.
}
